@model IEnumerable<TaRge25Shop.Models.Kindergarden.KindergardenIndexViewModel>
@{
    int rowNumber = 1;
}
<div>
    <a asp-action="Create" asp-controller="Kindergarden" type="submit" class="btn btn-primary">Create Kindergarden</a>
</div>
<div class="h1">
    <h1>Kindergardens</h1>
</div>

<form method="get">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Nr</th>
                <th scope="col">Group Name</th>
                <th scope="col">Children Count</th>
                <th scope="col">Create Date</th>
                <th scope="col">Teacher Name</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var kindergarden in Model)
            {
                <tr>
                    <td>@(rowNumber++)</td>
                    <td>@kindergarden.GroupName</td>
                    <td>@kindergarden.ChildrenCount</td>
                    <td>@kindergarden.CreatedAt</td>
                    <td>@kindergarden.TeacherName</td>
                    <td>
                        <a asp-action="Details" asp-controller="Kindergarden" asp-route-id="@kindergarden.Id" class="btn btn-sm btn-outline-primary">Details</a>
                        <a asp-action="Update" asp-controller="Kindergarden" asp-route-id="@kindergarden.Id" class="btn btn-sm btn-outline-primary">Update</a>
                        <a asp-action="Delete" asp-controller="Kindergarden" asp-route-id="@kindergarden.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>
